<idea-plugin>
    <id>com.boksh.add-python-sdk</id>
    <name>Add Python SDK</name>
    <vendor email="bokshitsky@gmail.com">Evgeny Bokshitsky</vendor>
    <description>Adds an "Add Python SDK" action to the Project View context menu of virtualenv folders (.venv, venv, .virtual_env, .virtualenv). Registers the interpreter as a Python SDK and assigns it to the containing module.</description>
    <depends>com.intellij.modules.platform</depends>
    <depends>PythonCore</depends>

    <extensions defaultExtensionNs="com.intellij">
        <notificationGroup id="Add Python SDK" displayType="BALLOON"/>
    </extensions>

    <actions>
        <action id="AddPythonSdk.AddPythonSdkAction"
                class="com.boksh.addpythonsdk.AddPythonSdkAction"
                text="Add Python SDK"
                description="Register this virtualenv as a Python SDK and assign it to the module">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
        </action>

        <group id="AddPythonSdk.AddPythonSdkGroup"
               class="com.boksh.addpythonsdk.AddPythonSdkGroup"
               text="Add Python SDK"
               popup="true">
            <add-to-group group-id="ProjectViewPopupMenu" anchor="last"/>
            <action id="AddPythonSdk.AddPoetrySdkAction"
                    class="com.boksh.addpythonsdk.AddPoetrySdkAction"
                    text="From Poetry"
                    description="Resolve the active Poetry environment (poetry env info) and assign it to the module"/>
        </group>
    </actions>
</idea-plugin>
